# Build artifacts staged for copying to the Mac / hardware; not source.
/artifacts/

# Build byproducts at the superproject root (regenerated by buildlinux.sh /
# the emulator; not source).
/griffin.dtb
/audio.raw
/cf.img
/rootfs.erofs

# Claude Code session-local state, not project source.
/.claude/

# Third-party build trees (extracted from dl/ tarballs; not source).
/build/
/dl/
